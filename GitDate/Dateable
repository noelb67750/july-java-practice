import java.util.List;

public interface Dateable {
    void Date(UserProfile user)
    void UnDate(UserProfile user);
    List<UserProfile> getDatingHistory();
    List<UserProfile> getDatingPreferences();
}